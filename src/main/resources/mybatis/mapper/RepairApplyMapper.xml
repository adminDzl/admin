<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="RepairApplyMapper">

	<resultMap type="com.wolves.dto.user.RepairApplyDTO" id="ResultMap">
		<id column="repair_apply_id" property="repairApplyId"/>
		<result column="apply_content" property="applyContent"/>
		<result column="apply_status" property="applyStatus"/>
		<result column="create_time" property="createTime"/>
	</resultMap>

	<resultMap type="com.wolves.dto.user.RepairApplyDetailDTO" id="DetailResultMap">
		<result column="apply_content" property="applyContent"/>
		<result column="image_urls" property="imageUrls"/>
		<result column="apply_status" property="applyStatus"/>
		<result column="create_time" property="createTimne"/>
	</resultMap>

	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into REPAIR_APPLY(
			USER_ID,
			APPLY_CONTENT,	
			IMAGE_URLS,	
			APPLY_STATUS,	
			CREATE_TIME,	
			UPDATE_TIME,	
			REPAIR_APPLY_ID
		) values (
			#{USER_ID},
			#{APPLY_CONTENT},	
			#{IMAGE_URLS},	
			#{APPLY_STATUS},	
			#{CREATE_TIME},	
			#{UPDATE_TIME},	
			#{REPAIR_APPLY_ID}
		)
	</insert>
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from REPAIR_APPLY
		where 
			REPAIR_APPLY_ID = #{REPAIR_APPLY_ID}
	</delete>
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  REPAIR_APPLY
			set
				USER_ID = #{USER_ID},
				APPLY_CONTENT = #{APPLY_CONTENT},
				IMAGE_URLS = #{IMAGE_URLS},
				APPLY_STATUS = #{APPLY_STATUS},
				CREATE_TIME = #{CREATE_TIME},
				UPDATE_TIME = #{UPDATE_TIME},
			REPAIR_APPLY_ID = REPAIR_APPLY_ID
			where 
				REPAIR_APPLY_ID = #{REPAIR_APPLY_ID}
	</update>
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
			USER_ID,
			APPLY_CONTENT,	
			IMAGE_URLS,	
			APPLY_STATUS,	
			CREATE_TIME,	
			UPDATE_TIME,	
			REPAIR_APPLY_ID
		from 
			REPAIR_APPLY
		where 
			REPAIR_APPLY_ID = #{REPAIR_APPLY_ID}
	</select>
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
			a.USER_ID,
			a.APPLY_CONTENT,
			a.IMAGE_URLS,
			a.APPLY_STATUS,
			a.CREATE_TIME,
			a.UPDATE_TIME,
			a.REPAIR_APPLY_ID,
			u.NAME
		from 
			REPAIR_APPLY a
		LEFT JOIN USER u ON (u.USER_ID = a.USER_ID)
	</select>
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
			a.USER_ID,
			a.APPLY_CONTENT,
			a.IMAGE_URLS,
			a.APPLY_STATUS,
			a.CREATE_TIME,
			a.UPDATE_TIME,
			a.REPAIR_APPLY_ID
		from
			REPAIR_APPLY a
	</select>
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from REPAIR_APPLY
		where
		REPAIR_APPLY_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>

	<!--查询我的报修列表-->
	<select id="selectRepairApplyByUserId" parameterType="com.wolves.dto.user.RepairApplyDTO" resultMap="ResultMap">
		SELECT
			repair_apply_id,
			apply_content,
			apply_status,
			create_time
		FROM repair_apply
		WHERE user_id = #{userId}
		ORDER BY create_time DESC
		LIMIT #{start}, #{size}
	</select>

	<!--根据id查询详情-->
	<select id="selectRepairApplyById" parameterType="com.wolves.dto.user.RepairApplyDetailDTO" resultMap="DetailResultMap">
		  SELECT
			  apply_content,
			  image_urls,
			  apply_status,
			  create_time
		  FROM repair_apply
		  WHERE repair_apply_id = #{repairApplyId}
	</select>

	<!--保存-->
	<insert id="saveRepair" parameterType="com.wolves.entity.system.Repair">
		insert into REPAIR_APPLY(
		USER_ID,
		APPLY_CONTENT,
		IMAGE_URLS,
		APPLY_STATUS,
		CREATE_TIME,
		UPDATE_TIME,
		REPAIR_APPLY_ID
		) values (
		#{userId},
		#{applyContent},
		#{imageUrls},
		#{applyStatus},
		now(),
		now(),
		#{repairApplyId}
		)
	</insert>
</mapper>